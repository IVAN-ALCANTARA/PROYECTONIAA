<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.tesoem.isc.niaa.proyectoniaa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".introduccionActivity"></activity>
        <activity android:name=".AcercaActivity" />
        <activity android:name=".FocusActivity" />
        <activity android:name=".GtActivity" />
        <activity android:name=".RaptorActivity" />
        <activity android:name=".FiestaActivity" />
        <activity android:name=".ShelbyActivity" />
        <activity android:name=".EdgeActivity" />
        <activity android:name=".MENUActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>